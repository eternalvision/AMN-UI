import {
    DarkModeProvider,
    useDarkMode,
} from "./DarkModeContext/DarkModeContext";
import {
    LanguageProvider,
    useLanguage,
} from "./LanguageContext/LanguageContext";

export const Context = {
    DarkModeProvider,
    useDarkMode,
    LanguageProvider,
    useLanguage,
};
